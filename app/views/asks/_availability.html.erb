<% days = days_of_week %>
<% days.each do |day| %>
  <strong><%= day.pluralize%>:</strong>
  <% if meetup_times = @ask.meetup_times.where("day = ?", day) %>
    <% times = [] %>
    <% meetup_times.each { |meetup| times << meetup.period } %>
    <%= times.join(', ') %>
  <% else %>
    none
  <% end %>
    <br>
<% end %>
