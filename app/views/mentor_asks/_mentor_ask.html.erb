<% @asks.each do |ask| %>
  <tr id="ask_<%=ask.id %>">
    <td><%= ask.categories.map {|category| category.name}.join ', ' %></td>
    <td><%= ask.description %></td>
    <td>
      <%= link_to "See Availability", mentor_ask_path(ask),
        class: "btn btn-info" %>
    <td>
      <div>
        <%= link_to 'Sign Up! »', new_answer_path(ask_id: ask.id),
          id: "answer_ask_#{ask.id}", class: "btn btn-success btn-large" %>
      </div>
    </td>
  </tr>
<% end %>
